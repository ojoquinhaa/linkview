# Project instructions

## Git / commits

- When asked to commit or push, do it on the **current branch as-is** — including `main`. Do **not** auto-create or switch to a new branch first (this overrides the default "if on the default branch, branch first" behavior).
- Never commit or push to a branch other than the one currently checked out.
